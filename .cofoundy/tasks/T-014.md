# T-014 — qa: el gemelo que falta — baseline contra referencia EXTERNA
role: qa
blockedBy: [T-012, T-013]
scope.write: [src/stories/chat-sim/**, src/__tests__/chat-sim/**, .cofoundy/state/reports/**]
scope.read: [.cofoundy/specs/telegram-fidelity-fix.md, .cofoundy/context/agent-floor.md]

## Por qué existe esta tarea
**Todas las sondas del ciclo son de consistencia interna.** El fixture de capabilities verifica que
el renderer OBEDEZCA al adapter, no que el adapter tenga RAZÓN. Un valor incorrecto pasa el gemelo
perfectamente: el DOM cambia igual, sólo que hacia lo equivocado. Así se nos escapó el tick de
Telegram, y lo encontró el operador mirando una captura real.

## Alcance
Un **baseline de fidelidad** que compare nuestro render contra una **referencia externa capturada**,
no contra otra rama de nuestro propio adapter. Manual y de una sola vez está bien; automatizado es
mejor si sale barato.

Como mínimo: una tabla de aserciones observables por canal, cada una citando su fuente primaria del
spec (`colors.palette`, `chat.style`, assets, TL schema), y un test que falle si el adapter deja de
cumplirlas.

## Acceptance
1. Cada aserción cita su fuente. Sin fuente, no entra.
2. **Gemelo: revertir `receipt.states` de Telegram al tick simple DEBE romperlo.** Ese es el bug
   original; si el baseline no lo caza, no sirve.
3. Las 3 cosas que la investigación marcó **no verificadas** (color exacto del tick leído, gradiente
   del wallpaper vivo, silueta de la colita) quedan anotadas como tales, **no rellenadas**.
