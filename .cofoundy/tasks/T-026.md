# T-026 — integración: `<cf-chat-sim>` en la landing de Fovente, lado a lado
role: integration
blockedBy: []
scope.write: [~/cofoundy/products/fovente-landingpage/** (SOLO en rama nueva)]

## Objetivo
El operador quiere **ver** el engine en su landing real, en localhost, **al lado del `ChatDemo.astro`
que ya está en producción**, para juzgar el gap él mismo.

## ⚠️ Reglas duras de repo ajeno
- `products/*` va por PR. **Rama nueva** `feat/chat-sim-preview`. **NO** commitear a `main`, **NO** abrir PR.
- La landing es **Astro 7 sin React y sin `@cofoundy/ui`**. Enlazá el paquete **local** (`file:` al
  worktree `packages/ui-cto-20260904-1144`), NO `github:` — nuestro trabajo no está en `main` del paquete.
- **NO borres ni modifiques `ChatDemo.astro`.** Es lo que se compara. Si lo tocás, se pierde la comparación.

## Alcance
Una ruta nueva, ej. `/chat-sim-preview`, con los dos componentes uno al lado del otro:

| Izquierda | Derecha |
|---|---|
| `ChatDemo.astro` (producción) | `<cf-chat-sim>` con el **mismo guion**, adaptado |

Importá por los subpaths reales, que recién ahora resuelven: `@cofoundy/ui/chat-sim/element` y
`@cofoundy/ui/chat-sim/styles.css`.

## Lo que el operador ya sabe y no hay que disimular
El engine tiene **2 de las 10 capacidades** de `ChatDemo.astro`. Le faltan `brief` (tarjeta con
bullets), `stop` (la IA frena), `done` (cierre exitoso), rotación entre rubros, badge de IA on/off,
etiqueta de rubro y `reply-in`.

**Esto es una comparación honesta, no una demo de ventas.** Poné arriba de cada panel una lista
corta de qué capacidades tiene y cuáles no. Que el gap se vea.

## Acceptance
1. `npm run dev` levanta y la ruta renderiza los dos paneles.
2. El de la derecha usa los **subpaths del paquete**, no rutas relativas — si el `exports` estuviera
   mal, tiene que fallar acá, que es donde importa.
3. `ChatDemo.astro` **sin diff**.
4. La rama **no** está mergeada ni tiene PR abierto.
5. Reportá el **puerto** de localhost.
