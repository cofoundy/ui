# T-005 — channel: adapters WhatsApp + Telegram
role: channel
blockedBy: [T-003]
scope.write: [src/components/chat-sim/adapters/**]
scope.read: [.cofoundy/specs/adapter-interface-draft.md]
## Alcance
Los **16 campos**, cero opcionales. `caps.ts` es **hoja y no importa nada**; `registry.ts` importa
los adapters (si se fusionan se cierra el ciclo de imports, igual que en el Python de inbox-ai).
`normalizeReactionEmoji()` UNA sola vez. `validateScript()` en compile-time.
## Acceptance
1. `receipt:'delivered'` en Telegram ⇒ `Diagnostic`. **Gemelo positivo:** el mismo guion en
   WhatsApp ⇒ diagnostics vacío.
2. Emoji fuera de la allowlist de 73 ⇒ `Diagnostic`. Con y sin U+FE0F ⇒ **mismo resultado**.
3. `ChannelId` vive acá (movido desde T-006).
4. El grep de `channel ===` es señal secundaria, **no el detector**: no caza `switch`, `==`, ni
   lookups por clave literal, y sobre todo no caza el hardcode más probable (dibujar el doble-tick
   sin rama alguna). **El detector primario es el fixture de caps de T-002 #6**, que corre en la
   ola 1 — antes de que `capture/` congele goldens en la ola 2.
