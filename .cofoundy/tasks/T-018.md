# T-018 — channel: migrar los adapters al enum de iconos
role: channel
blockedBy: [T-016]
scope.write: [src/components/chat-sim/adapters/**]

## Origen
**Tercer hueco del CTO del mismo tipo, y el segundo en este arreglo.** T-016 cambió
`glyph: string` → `glyph: ReceiptIconId`. Asigné `element/**` a T-017 y **volví a olvidar los
adapters** — T-012 ya había cerrado y no tiene `blockedBy T-016`.

E-002 se resolvió con la frase "la matriz necesita otra pasada". La hice, y repetí el error.
La lección real está abajo.

## Alcance
`adapters/telegram.ts` y `adapters/whatsapp.ts` pasan de caracteres literales a `ReceiptIconId`:

| Antes | Ahora |
|---|---|
| `'🕐'` | `'clock'` |
| `'✓'` | `'check'` |
| `'✓✓'` | `'double-check'` |
| `'!'` | `'alert'` |

**No cambian los VALORES semánticos.** Telegram sigue cambiando glifo con color fijo; WhatsApp
sigue cambiando color con glifo fijo. Solo cambia cómo se nombra el glifo.

`adapters/caps.ts` **no se toca**: su allowlist de 73 emoji de reacciones de Telegram son datos
de dominio legítimos, no glifos de render. El escaneo de T-016 ya la excluye.

## Acceptance
1. Cero emoji en `adapters/{telegram,whatsapp}.ts`. El `it.fails` de `emoji-scan.test.ts` baja de
   5 violaciones a las que queden fuera de tu celda.
2. **Los dos gemelos de ortogonalidad siguen verdes sin tocarlos.** Si tuviste que editarlos, el
   cambio no fue de nomenclatura y quiero saberlo.
3. `tsc` y `madge` limpios en `adapters/**`.
