# T-029 — core: `capabilities` es el 17º campo del contrato
role: core
blockedBy: []
scope.write: [src/components/chat-sim/core/**]

## La decisión y por qué
`channel` (T-028) paró antes de forzar: agregar `capabilities` al objeto que devuelve `getAdapter`
rompe `core/__tests__/exports.test.ts`, que afirma **exactamente 16 campos, ni uno más**.

Dos salidas. **Se elige la 2:**

1. ❌ Relajar el test a "incluye al menos estos 16". **Debilita un instrumento**: ese test es lo que
   impide que la superficie crezca en silencio — justo lo que el audit marcó con el `export *`.
2. ✅ **`capabilities` es un 17º campo real de `ChannelAdapter`.** Describe qué puede hacer el canal:
   misma categoría que los otros 16, no un anexo. El shim
   `ChannelAdapterWithCapabilities extends` que `channel` armó deja de hacer falta.

## Consumidores — grep corrido ANTES de escribir esto
`grep -rl "ChannelAdapter" src/` da 13 archivos. **Los 4 que CONSTRUYEN un literal y rompen:**

| Archivo | Dueño |
|---|---|
| `element/fixtures.ts` | skin — avisado |
| `element/chat-sim-element.ts` | skin — avisado |
| `react/__tests__/receipt-model.test.tsx` | **app — T-030** |
| `react/__tests__/reactions.test.tsx` | **app — T-030** |

`react/MessageThread.tsx` solo consume; no rompe.

*(Es la quinta vez del patrón "cambio de contrato, consumidores huérfanos". Las 4 anteriores el grep
lo corrí después. Esta vez antes, y los 2 de `react/` eran justo los que se me escapaban.)*

## Alcance
`capabilities: Readonly<Record<Capability, Constraint | null>>` como campo requerido. El tipo
`Capability` es `'buttons' | 'list'` — **`REACTION` queda afuera a propósito**: ya tiene sus campos
`reactions`/`reactionConstraint`, y meterlo acá sería una segunda fuente de verdad.

## Acceptance
1. `exports.test.ts` afirma **exactamente 17** — sigue siendo igualdad, no "al menos".
2. Un adapter sin `capabilities` **no compila**. Gemelo con `@ts-expect-error`.
3. `tsc` y `madge` limpios en `core/**`.
