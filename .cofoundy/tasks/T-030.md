# T-030 — app: los 2 fixtures de react al contrato de 17 campos
role: app
blockedBy: [T-029]
scope.write: [src/components/chat-sim/react/**]

## Origen
`capabilities` pasa a ser el 17º campo de `ChannelAdapter` (T-029). Estos dos archivos **construyen
adapters literales** y no compilan sin él:

- `react/__tests__/receipt-model.test.tsx`
- `react/__tests__/reactions.test.tsx`

`react/MessageThread.tsx` solo consume — no se toca.

**Los encontró el `grep -rl` corrido ANTES de repartir las tareas**, no un typecheck roto después.
Es la quinta vez del patrón; las 4 anteriores el grep se corrió tarde.

## Alcance
Agregar `capabilities` a los dos fixtures. **Es nomenclatura de contrato, no semántica**: si te ves
cambiando lo que los tests afirman, parálo — el cambio dejó de ser mecánico.

## Acceptance
1. Los 2 archivos compilan; sus aserciones **sin editar**.
2. Suite de `react/**` verde.
