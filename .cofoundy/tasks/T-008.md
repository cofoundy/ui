# T-008 — qa: stories, tests, MobileBaseline, docs
role: qa
blockedBy: [T-006, T-007]
scope.write: [src/stories/chat-sim/**, src/__tests__/chat-sim/**, COMPONENTS.md]
## Alcance
Stories por canal y por estado. `MobileBaseline` para todo lo nuevo (hoy solo 4 de 98 stories la
tienen). Story de reemplazo del bubble de inbox-ai.
## Acceptance
1. **La story de reemplazo se tipa contra `inbox-ai/frontend/src/lib/api.ts:229` (`interface
   Message`)**. Sin escape hatch: si el tipo no calza, la story no compila. *(El paréntesis "o
   adaptador inline" disolvía el criterio y se elimina.)*
2. `MobileBaseline` presente en cada componente interactivo nuevo.
3. Coverage de `chat-sim/**` ≥80%.
4. `COMPONENTS.md` actualizado. La anotación de deuda en `src/types/**` se pide al CTO por `A` —
   esa celda es ⛔ para `qa` en la matriz; escribirla sería violación de scope.
5. **Cálculo de N publicado**: `N KB reemplazables` = suma de bytes de los bloques de
   `inbox-ai/.../conversation/MessageBubble.tsx` que la story cubre, con el comando que lo mide en
   el reporte. Sin N calculado, el criterio de éxito #4 no tiene número.
