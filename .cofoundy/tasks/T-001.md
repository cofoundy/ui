# T-001 — core: timeline mínima + playhead
role: core
blockedBy: []
scope.write: [src/components/chat-sim/core/**, src/components/chat-sim/index.ts, package.json (solo campo exports)]
scope.read: [.cofoundy/specs/api-contract.md, .cofoundy/specs/architecture-v1.md §1 §13]

## Alcance
`compile()` con PRNG **posicional** (`rand(seed, stepIdx, slot)`), `Frame[]` + `Int32Array keys`,
playhead por rAF con `pause`/`rate` reactivos. Eventos de esta tarea: `post`, `draft`, `flag`.
`tz` es parámetro OBLIGATORIO de `compile` (§13) y entra al `digest`.

## Acceptance (falsable)
1. `pnpm vitest run chat-sim/core` verde.
2. Test de pureza: `seek(tl,t)` dos veces con el mismo `t` ⇒ deep-equal.
3. Test cross-machine: dos `compile()` con distinto `tz` ⇒ **digest distinto**. Gemelo positivo:
   mismo `tz` ⇒ digest idéntico.
4. `duration === frames.at(-1).t`. No existe estimador paralelo en el código.
5. Lint de pureza activo: un `Math.random()` en `core/` **rompe CI**. Demostralo con un commit
   temporal que falle y revertilo — el instrumento tiene que poder ponerse rojo.
