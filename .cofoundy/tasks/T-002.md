# T-002 — skin: stylesheet scoped + element + layout WhatsApp
role: skin
blockedBy: [T-001]
scope.write: [src/components/chat-sim/element/**, src/components/chat-sim/styles.css]
scope.read: [.cofoundy/specs/architecture-v1.md §12 §4, .cofoundy/specs/adapter-interface-draft.md,
             .cofoundy/specs/api-contract.md, .cofoundy/specs/file-ownership-matrix.md,
             src/components/chat-sim/core/types.ts]

## Alcance
`<cf-chat-sim>` con **`data-step` en la raíz** (NO clases acumuladas — el fold es no monótono,
§13). Pre-render del hilo completo + revelado por `data-step`. Layout WhatsApp: burbuja, colita en
la **primera** de la racha, `.stamp`/`--pad`, ticks, wallpaper. Más `demo/index.html`.

## Referencia de estilo — OBLIGATORIA
`products/fovente-landingpage/src/components/ChatDemo.astro` y su `src/styles/tokens.css`. Es
nuestro lenguaje visual, en producción, y la continuidad con él es **deliberada**: no la rompas.
NO es referencia el repo `whatsimule` (ver A-2: cero copia literal, y si creés necesitar un lift
literal, PARÁ y escalá).

## Acceptance (falsable)
1. `demo/index.html` abre y reproduce un guion real. **Este es el artefacto que el CTO abre con
   `open` — R-1.**
2. Todos los tokens bajo `.cf-chat-sim`. **Cero** declaraciones nuevas en `:root`.
3. **Cero** utilidades Tailwind en `chat-sim/**`: el lint las rechaza. Gemelo positivo: agregá una
   `flex` temporal, verificá que CI falla, revertí.
4. `git diff --exit-code src/index.ts src/styles/index.css` contra el merge-base ⇒ **sin cambios**.
5. Renderiza idéntico servido como HTML plano, sin Tailwind del host.
6. **Fixture de capabilities (detector de hardcode, ola 1).** Renderizá con un adapter FALSO que
   invierte **cuatro ejes estructurales** ⇒ el DOM **debe cambiar en los cuatro**:
   `tail:'last'` · `receiptGlyph:'single-tick'` · `timestamp:'inside-plain'` · `reactions:'own-row'`.

   El adapter falso **tipa contra `ChannelAdapter` de `core/types.ts`** (T-001) — lectura pura, sin
   forkear el tipo. Esto es lo que hace la sonda ejecutable en la ola 1.

   **`wallpaper` NO entra**: es `pattern` en WhatsApp *y* en Telegram, así que no discrimina entre
   los dos canales que este ciclo implementa, y es fondo — lo más cercano a "sólo color", que es
   justo lo que el criterio de éxito #2 descalifica.

   **`timestamp` es el eje crítico**: WhatsApp es `inside-pad` y **reserva `--pad`**, que es
   exactamente lo que mide el settle gate de T-004. Un `inside-pad` hardcodeado se congela en los
   goldens de la ola 2 y el `inside-plain` de Telegram lo descubre en la ola 3, invalidándolos.

   Un layout que dibuja el doble-tick sin ninguna rama por canal pasa cualquier grep con cero
   ocurrencias; este fixture lo caza. Instrumenta además el criterio de éxito #2.
7. **Cero React en el bundle de `element/`**: assert sobre el output del build — `react` y
   `react-dom` ausentes del grafo de módulos. **Gemelo positivo ejecutable en la ola 1** (no contra
   `react/`, que no existe hasta la ola 4): un import temporal de `react` en `element/` **debe**
   romper el assert. Agregalo, verificá el rojo, revertí.
8. **`wall_clock_minutes: <n>` en la primera línea de `.cofoundy/state/reports/skin.md`.** Sin el
   dato, el threshold de presupuesto es decorativo.
