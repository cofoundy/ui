# T-007 — app: renderer React + mobile
role: app
blockedBy: [T-005]
scope.write: [src/components/chat-sim/react/**]
scope.read: [.cofoundy/specs/api-contract.md, .cofoundy/specs/file-ownership-matrix.md, .cofoundy/specs/architecture-v1.md, .cofoundy/context/agent-floor.md]
## Alcance
`<ChatSim script channel seed mode="demo"|"live" />` sobre el mismo `SimState` del core. Mobile:
`visualViewport`, `100dvh`, `interactive-widget=resizes-content`, safe-area, tap ≥44px,
input ≥16px.
## Acceptance
1. Snapshot cruzado: `element` y `react` en el mismo `t` producen el **mismo DOM semántico**.
2. **Test mecánico**, no prosa: con `visualViewport` simulado a 375×(alto−336), el composer queda
   dentro del viewport visible y el último mensaje sigue visible. Gemelo: sin el fix, el test falla.
3. **Checklist derivada del `MobileComposer.tsx` real** (13 KB, inbox-ai): por cada razón que ese
   fork declara para existir, una fila con veredicto cubierto/no-cubierto. "Queda sin razón de ser"
   deja de ser opinión y pasa a ser una tabla con N filas cubiertas de N.

## Telemetría (obligatoria)
`wall_clock_minutes: <n>` en la primera línea de `.cofoundy/state/reports/app.md`.
Sin el dato, `budget_overrun` es infireable y el threshold queda decorativo.
