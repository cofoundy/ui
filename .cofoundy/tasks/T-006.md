# T-006 — skin: sound packs + token iMessage
role: skin
blockedBy: [T-005]
scope.write: [src/components/chat-sim/sound/**, src/components/chat-sim/styles.css]
## Alcance
Schema declarativo `{id, layers:[{freq|[from,to], startMs, durMs, jitterHz?, wave, attackMs}],
gain, repeat?}`. Cue packs de **WhatsApp y Telegram como DATOS**. `wave` incluye **noise**.
Envolvente y attack **por capa**. `AudioSink` con schedule-and-cancel (frena en cualquier seek),
muteado por default. Story de audición. Más `--channel-imessage` y el `ChannelId` (NO el adapter).
**Tope de costo (A-1): si excede ~0,5 d, revertí a stub y avisá.**
## Acceptance
1. Un `seek` durante un cue agendado lo **cancela**; no queda cola sonando.
2. Cero samples de terceros: todo sintetizado.
3. Story de audición reproduce los cues de ambos canales y suenan distinto.
