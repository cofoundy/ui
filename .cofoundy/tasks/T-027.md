# T-027 — paridad con `ChatDemo.astro`: scroll, loop, service, card, branded
role: skin
blockedBy: []
scope.write: [src/components/chat-sim/element/**, src/components/chat-sim/styles.css, demo/**]

## Método antes que alcance
Mi lista de "2 de 10" salió de leer los **tipos de paso** del markup y se me escapó el auto-scroll,
que es una función aparte. **Antes de estimar, corré `ChatDemo.astro` y listá comportamientos
OBSERVADOS, no declarados.** Si encontrás más de lo que está acá, decímelo.

## A — Auto-scroll (el que falta y no estaba en mi lista)
`core` **ya expone `scrollId`** (`types.ts:269`) y el fold lo mantiene. **Ningún renderer lo
consume.** Está el dato y falta el efecto.

Referencia: `glide()` en `ChatDemo.astro` — rAF, ease cúbico, 220 ms, más un catch-up a 260 ms.
Al hacer `seek` debe ser **instantáneo**, no animado (arch §1 clase 9).

## B — Loop
Producción encadena rubro tras rubro para siempre. El nuestro reproduce un guion y para.

## C — `service` — mensaje de servicio centrado
Cubre `stop` ("FOVENTE SE DETUVO · PAGO DECLARADO") y `done`. **No es vocabulario de Fovente:**
WhatsApp tiene el aviso de cifrado y Telegram el divisor de no-leídos. Ya renderizamos uno — la
píldora de fecha. Generalizalo con variante (`neutral` / `warn` / `success`).

## D — `card` — tarjeta estructurada
Cubre el `brief` (título + bullets + acción). Tampoco es de Fovente: WhatsApp tiene tarjetas de
contacto y de ubicación, y el adapter ya declara `senderKinds`.

## E — `chrome: 'branded'` — el tercer modo
Descubierto comparando: producción usa la **paleta de marca de Fovente** (burbuja saliente roja
brasa), no la de WhatsApp. No simula un canal: es Fovente con forma de chat.

| Modo | Qué fija |
|---|---|
| `fidelity` | se ve como el canal real |
| `consistent` | controles en posición fija |
| **`branded`** | **paleta del consumidor**, estructura del canal |

En `branded` la **estructura sigue siendo del canal** — colita, ticks, agrupación. Solo cambia la
paleta. **Gemelo: en `branded`, WhatsApp y Telegram siguen difiriendo estructuralmente.**

## Acceptance
1. El hilo sigue al último mensaje al reproducir; **instantáneo al seek**. Gemelo: desconectar
   `scrollId` debe romperlo.
2. El loop reinicia sin recrear nodos — el bug del typing de T-017 no debe volver. Gemelo: contar
   nodos entre dos vueltas.
3. `service` y `card` renderizan en los dos canales, con **estructura por adapter**, no hardcodeada.
4. `branded` acepta tokens del consumidor y **no altera la estructura**. Gemelo arriba.
5. Los 8 gates del ciclo siguen verdes.
