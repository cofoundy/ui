# T-004 — capture: PNG determinista + settle gate
role: capture
blockedBy: [T-002, T-003]
scope.write: [src/components/chat-sim/capture/**, scripts/capture-chat.mjs]
scope.read: [.cofoundy/specs/api-contract.md, .cofoundy/specs/file-ownership-matrix.md, .cofoundy/specs/architecture-v1.md, .cofoundy/context/agent-floor.md]
## Alcance
CLI headless sobre `demo/index.html`. Settle gate: `document.fonts.ready` + medición de `--pad`
estable + `cf-static` (desactiva springs, §1 clase 4) antes de disparar.
## Acceptance
1. Dos corridas con mismo `(script,seed,channel,locale,tz)` ⇒ **PNG byte-idéntico** (`sha256`).
2. **Gemelo positivo:** cambiar la `seed` ⇒ el byte-compare DEBE romperse. Un test que no puede
   fallar no mide nada.
3. Corre en CI headless, no solo en la máquina del autor.

## Telemetría (obligatoria)
`wall_clock_minutes: <n>` en la primera línea de `.cofoundy/state/reports/capture.md`.
Sin el dato, `budget_overrun` es infireable y el threshold queda decorativo.
